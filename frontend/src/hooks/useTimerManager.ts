/**
 * Thin convenience wrapper around the global TimerContext.
 * Import this hook in any component that needs to interact with timers.
 */
export { useTimerContext as useTimerManager } from '../context/TimerContext';
